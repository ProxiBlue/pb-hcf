# Task 011: Rector enforcement wiring

**Status**: pending
**Depends on**: 010
**Retry count**: 0

## Description
Wire rector into the pipeline: (a) deterministic dry-run check documented for post-batch/pre-commit use (script scripts/rector-check.sh — changed files only, non-empty diff = exit 1), (b) update pre-commit-adversarial-pass agent to consume rector output as structured input, (c) document rector.php as generation-time convention context for tdd-workers (wire adds pointer to .claude/CLAUDE.md fence).

## Context
- scripts/rector-check.sh: `git diff --name-only <ref> -- 'app/code/**/*.php'` (custom-code root; exclude generated/, vendor/, fixtures — SAME scoping convention as tasks 005/012, no separate "namespace config" source) | xargs rector --dry-run --config; exit 1 with diff on findings; shellcheck-clean; degrade exit 0 with note when rector absent
- Agent update: agents/pre-commit-adversarial-pass.md gains a rector-diff review step (judge contested transforms rather than style opinions); do NOT create a new agent — standards-enforcer is HCF-owned (untouchable), our adversarial-pass is the pb-hcf seat for this
- **Wire fence (wire SKILL.md edit — coordination):** the fence is generated by wire SKILL.md step 2 (the fenced-section template). Add ONE pointer line to `.claude/rector.php` there. Re-read SKILL.md first (tasks 002/006/008 may have edited other sections); touch ONLY the step-2 fence template. Note: rector.php is a TEMPLATE, not a playbook — the pointer is a plain line in the fence, not a playbook auto-discovered by the `templates/playbooks/` glob.

## Requirements (Test Descriptions)
- [ ] `it ships scripts/rector-check.sh shellcheck-clean scoped to changed custom-namespace files`
- [ ] `it exits 1 with the transform diff when rector proposes changes`
- [ ] `it exits 0 with an install note when rector is absent`
- [ ] `it extends pre-commit-adversarial-pass with a rector-diff judgment step`
- [ ] `it adds the rector convention pointer to the wire CLAUDE.md fence`

## Acceptance Criteria
- Deterministic layer (script) and judgment layer (agent) cleanly separated
